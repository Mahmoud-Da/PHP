*** 1-Introduction ***

-------------------code----------------------
• PHP was launched in 1995.
• nowadays it's powering about 80% of all websites.
• Popular software is also written in it, for example like WordPress for blogging.
• PHP in general is an incredibly powerful language.
• Also, another advantage is that PHP web hosting is incredibly cheap.
-------------------code----------------------

- we might even find free Hosts or even Hosts for just a few dollars, so it's one of the most affordable
hosting options for websites Moreover.

- PHP has evolved significantly over the last few years, and it used to be a pure scripting
language for simple scripts, and for some people, it might still have a somewhat bad reputation due
to that, but it's no longer true. It has become an advanced, object oriented programming language that we can utilize for modern applications,

- So what's the structure of this course?
Well, first we will just set up our system.
Then we will examine how we can use PHP for small projects.
So there we will have a look at variables conditionals like if and else loops and arrays and how all
of that works.
Then we will go to to PHP for intermediate projects.
So there we will examine how we can work with a database, how functions work, how we can add type
annotations to those functions to make them more, , easy to use, how to handle file uploads and
all of those things is what we will examine then.
And then we will have a look at how we can use PHP for larger projects.
So there we will investigate things like object oriented programming, namespaces, auto-loading, the
MVC pattern, and all of those things.
-------------------code----------------------
• set up our system.
• PHP for small projects
• PHP for intermediate projects
• PHP for larger projects
-------------------code----------------------

*** 4-Install XMAPP (mac OS) ***

1.Downloading XAMPP
- Search for XAMPP on Google and go to the Apache Friends website.
- XAMPP is a bundle that includes:
  - Apache (Web Server)
  - MariaDB (Database, alternative to MySQL)
  - PHP (Server-side scripting language)
  - Perl (Optional, not required but included)
- Click the download button for Mac (OS X) and confirm the download.
-------------------code----------------------
https://www.apachefriends.org/
-------------------code----------------------

2.Installing XAMPP
- Once downloaded, open the installer.
- If you see a security warning saying "Developer cannot be verified," do the following:
  - Press Shift + Right Click on the installer and select Open.
  - Click Open to proceed.
- Enter your Mac password when prompted to allow installation.
- Follow the installation steps:
  - Default installation path: `/Applications/XAMPP`
  - Click Next, Next and wait for the installation to complete.
- Click Finish, and XAMPP should launch automatically.

3.Running XAMPP
- After installation, XAMPP will open.
- You can check if it is working by opening a browser and visiting:
  - `http://localhost/dashboard`
- If the page loads successfully, XAMPP is running properly.

4.Starting and Managing Services
- Open the XAMPP Manager to control servers.
- Ensure the following are running:
  - Apache (Web server)
  - MariaDB (Database server)
- If MariaDB is not running:
  - Select MariaDB and click Start.
  - Wait for the service to start.
  - Refresh `phpMyAdmin` (`http://localhost/phpmyadmin`) to confirm it is working.

5.Troubleshooting
- If Apache or MariaDB fails to start, refer to the troubleshooting section in the course.
- Ensure no other applications are using the same ports.
- Restart XAMPP if needed.

With these steps, XAMPP should be successfully installed and running on your Mac.
PS: if the sql database doesn't worked properly 
method1:
- show the proses of sql 
-------------------code----------------------
ps aux | grep mysql
-------------------code----------------------

kill the proses
-------------------code----------------------
kill -9 {process id}
-------------------code----------------------

method2:
-------------------code----------------------
sudo killall mysqld
sudo /Applications/XAMPP/xamppfiles/bin/mysql.server start
-------------------code----------------------

In the kill command, option 9 (i.e., kill -9) is used to send the SIGKILL signal to a process.
This forcefully terminates the process without allowing it to clean up resources or save its state.

*** 5. Important fix: XAMPP (macOS) ***

-------------------code----------------------
display_errors=On
error_reporting=E_ALL
-------------------code----------------------

By default, PHP is configured for production, meaning errors are not displayed. To enable error reporting:
Navigate to:
-------------------code----------------------
/Applications/XAMPP/etc/php.ini
-------------------code----------------------

Open php.ini in a text editor (e.g., TextEdit or VS Code).

Locate the following directives and update them:
-------------------code----------------------
display_errors = On
error_reporting = E_ALL
-------------------code----------------------

Save the file and close the editor.

Restart the XAMPP web server for changes to take effect.
